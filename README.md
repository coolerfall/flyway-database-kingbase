# flyway-database-kingbase
Flyway database support for Kingbase

## Usage

Add the dependency in your `build.gradle`:

```groovy
implementation 'com.coolerfall.flyway:flyway-database-kingbase:10.22.0'
```
